# Frontend environment (Vite). Copy to `.env` and adjust.
# Base URL of the Privoraa Spring Boot backend. When unreachable, the app
# automatically falls back to a local demo engine so the UI still works.
VITE_API_BASE_URL=http://localhost:8099/api/v1
